// MATHS: is an objest (collection of difined var/methods)
    // Math always written with a capital M 
    // also to access math values use " Math.(var/value);"
    // Ex. Math.PI = 3.141592653589793
        // Math.floor(); = completly chops of decimals only leaving whole number no rounding
        // Math.ceil(); = auto rounds number UP regardless of decimal value
        // Math.pow(); = int selected to the power of intSelected / same as (int ** int)
            // Ex. Math.pow(3, 2); = 9 (3 to the power of 2)
        // 
        // 
    // Generating Random Numbers
        // with " Math.random() " = 0.3967596344411166 (always random decimal #)
        // if want whole # use Math.floor(Math.random() * numSeleted)
            // if want random num 1-5 must do above + 1 ("+1" so we dont get 0 as result plus get ability to get 5[which not possible witout it])
                // Ex. Math.floor(Math.random() * 5) + 1 = (will produce num 1 - 5)
            // if want only specified # of option use Math.floor(Math.random() * numOfOptions) + strtingDigitWanted
                // Ex. Math.floor(Math.random() * 3) + 20 = (will produce random num between "20 to 22" so only 3 option 20, 21, 22 and nothing else)
















